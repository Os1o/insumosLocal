# Denegar acceso directo al directorio
Options -Indexes

# Solo permitir acceso desde tu dominio
<IfModule mod_rewrite.c>
    RewriteEngine On
    # Bloquear acceso directo
    RewriteCond %{HTTP_REFERER} !^http(s)?://(www\.)?tudominio\.com [NC]
    RewriteRule .* - [F,L]
</IfModule>

# O simplemente denegar todo acceso directo
Order Deny,Allow
Deny from all